services:
  - type: web
    name: makeup-store-api
    env: node
    rootDir: server/src
    buildCommand: cd .. && npm install
    startCommand: cd .. && npm start
    envVars:
      - key: DB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
